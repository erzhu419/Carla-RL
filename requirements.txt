# CARLA RL Project Dependencies (PyTorch Migration)

# PyTorch (Install corresponding CUDA version if using GPU)
torch>=2.0.0
torchvision>=0.15.0
tensorboard>=2.10.0

# CARLA Python API (Must match server version)
# Recommended to install from .whl in CARLA_0.9.16/PythonAPI/carla/dist/
carla==0.9.16

# Standard dependencies
numpy>=1.20.0
opencv-python>=4.5.0
psutil>=5.8.0
colorama>=0.4.4
tqdm>=4.60.0
